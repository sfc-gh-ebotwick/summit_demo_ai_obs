name: app_environment
channels:
  - snowflake
dependencies:
  - bs4=*
  - snowflake-ml-python=*
  - snowflake.core=*
  - trulens-connectors-snowflake=*
  - trulens-core=*
  - trulens-feedback=*
  - trulens-providers-cortex=*
